{
  "name": "arxlens",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute arxlens-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute arxlens-db --file=./schema.sql --remote"
  },
  "dependencies": {
    "agents": "^0.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250515.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
